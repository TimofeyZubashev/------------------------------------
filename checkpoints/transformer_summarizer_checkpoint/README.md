# Transformer summarizer checkpoint

Local copy of the trained checkpoint exported from the Kaggle notebook.

Expected file:

```text
summarizer_checkpoint.pt
```

The `.pt` file is ignored by git via `.gitignore`, but it is available locally in this directory for inference and fine-tuning scripts.
